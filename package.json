{
  "name": "@singlecloud/ui",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "generate": "plop --plopfile generators/index.js",
    "lint:staged": "lerna run lint:staged"
  },
  "pre-commit": "lint:staged",
  "devDependencies": {
    "lerna": "^3.15.0",
    "plop": "^2.2.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "http-proxy-middleware": "0.20.0-beta.2"
  }
}
