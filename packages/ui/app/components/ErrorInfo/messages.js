/*
 * CommenWarningPage Messages
 *
 * This contains all the text for the CommenWarningPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CommenWarningPage';

export default defineMessages({
  warningTitle: {
    id: `${scope}.warningTitle`,
    defaultMessage: 'Operation Failure',
  },
});
