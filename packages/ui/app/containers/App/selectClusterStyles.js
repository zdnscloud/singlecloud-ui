const styles = (theme) => ({
  formControl: {
    margin: theme.spacing.unit,
    flexBasis: 200,
    minWidth: 194,
    marginRight: '2em',
  },
  selectEmpty: {
    marginTop: theme.spacing.unit * 2,
  },
});

export default styles;
